```js
<div>
  <Button
    type='primary'
    inner='Primary'
    onClick={() => alert('Primary Button clicked')}
  />

  <Button
    type='secondary'
    inner='Secondary'
    onClick={() => alert('Secondary Button clicked')}
  />

  <Button
    type='primary'
    inner='Disabled!'
    onClick={() => alert('Primary Button clicked')}
    disabled
  />

  <Button
    type='secondary'
    inner='Disabled!'
    onClick={() => alert('Secondary Button clicked')}
    disabled
  />
</div>
```
